- name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
          
      # NEW STEP: Install libraries from your file
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          
      - name: Run ETL Script
        run: python etl_script.py
